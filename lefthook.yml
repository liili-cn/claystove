# pre-commit:
#   parallel: false
#   commands:
#     format:
#       run: pnpm exec prettier {staged_files} --write
#       stage_fixed: true

commit-msg:
  commands:
    lint:
      run: pnpm exec commitlint --edit {1}
