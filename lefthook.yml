pre-commit:
  parallel: false
  commands:
    format:
      run: pnpm exec prettier --ignore-unknown --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    lint:
      run: pnpm exec commitlint --edit {1}
