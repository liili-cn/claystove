{
  "$schema": "https://turbo.net.cn/schema.json",
  "tasks": {
    "check-types": {
      "inputs": ["./src/**"],
      "outputs": ["./src/**"]
    },
    "build": {
      "dependsOn": ["^build", "check-types"],
      "inputs": ["./src/**"],
      "outputs": ["./dist/**"]
    }
  }
}
